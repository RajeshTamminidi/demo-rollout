apiVersion: v1
kind: Service
metadata:
  name: demo-app-svc
  namespace: demo-canary
spec:
  selector:
    app: demo-app
  ports:
    - port: 80
      targetPort: 5678
  type: LoadBalancer

